#include <iostream>
using namespace std;

int main() {
    int length;
    while(cin >> length){
        int* numList = new int[100];
        for (int i = 0; i < length; i++){
            cin >> numList[i];
        }
        
        for(int i = 0; i < length; i++){
            for (int j = 0; j < length - i - 1; j++){
                if(numList[j] > numList[j+1]){
                    int temp = numList[j];
                    numList[j] = numList[j+1];
                    numList[j+1] = temp;
                }
            }
        }
        for (int t = 0; t < length; t++){
            if (t < length - 1) cout << to_string(numList[t]) + " ";
            else cout << to_string(numList[t]) +"\n";
        }
        
    }
    return 0;
}
